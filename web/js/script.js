(function($){
    $.fn.pluginList = function(options) {
 
        // Options par defaut
        var defaults = {};
 
        var options = $.extend(defaults, options);
 
        this.each(function(){
 
            var obj = $(this);
 
            // Traitement
 
        });
        // On continue le chainage JQuery
        return this;
    };
})(jQuery);
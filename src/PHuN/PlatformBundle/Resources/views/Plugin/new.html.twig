{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Plugin creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('plugin') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
